"""Analytical layer.

Modules in this package consume the raw data deposited by
:mod:`src.ingestion` and produce tidy GeoDataFrames or summary tables in
``data/interim/`` and ``data/processed/``. They never call the network.
"""
